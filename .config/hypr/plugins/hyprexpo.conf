plugin {
    hyprexpo {
        columns = 3
        # gap_size = 0
        # bg_col = rgb(111111)
        workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1
        skip_empty = false
        gesture_distance = 150
        hyprexpo-gesture = 4, up, expo, on
        hyprexpo-gesture = 4, down, expo, off
    }
}