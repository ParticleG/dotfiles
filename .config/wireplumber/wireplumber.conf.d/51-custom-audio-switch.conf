# Custom audio profile auto-switching configuration
# Automatically switches between Headphones and Speaker profiles
# Priority: Headphones (when plugged) > Speaker > HDMI

wireplumber.components = [
  {
    name = device/custom-audio-switch.lua, type = script/lua
    provides = hooks.device.custom-audio-switch
    requires = [ policy.device.profile, policy.device.routes ]
  }
]
